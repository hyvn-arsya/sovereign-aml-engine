[pytest]
testpaths = .
# infrastructure tests run under infrastructure/.venv (aws_cdk); keep them out
# of the root-venv suite.  infrastructure/cdk.out is build output and stale
# copies shadow real source modules.
norecursedirs = infrastructure infrastructure/cdk.out .venv312 .venv .git __pycache__
